# Local `pnpm dev` reads live scores from the dev environment's public blob
# container (its CORS allows http://localhost:5173). Not used by tests/builds.
VITE_SCORES_BASE_URL=https://solthothfbpicksdev.blob.core.windows.net/scores
